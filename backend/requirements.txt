fastapi>=0.100.0,<0.120.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
celery[redis]==5.4.0
redis==5.1.0

# ✅ Pydantic v2 stack (compatible with Python 3.14)
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0

# LangChain — pydantic v2 compatible versions
langchain>=0.2.0,<0.4.0
langchain-core>=0.2.0,<0.4.0
langchain-text-splitters>=0.2.0,<0.4.0
# langchain-community removed — not used in codebase (was yanked 0.0.9)

# Vector DB — Qdrant Cloud via direct REST API (httpx)
# qdrant-client requires pydantic v2 and is available for Python 3.14 only in 1.7+
# We skip the client and call Qdrant REST API directly with httpx — no pydantic conflict.

# AI + utils
huggingface-hub>=0.27.0,<0.30.0
pypdf==4.3.0
psutil==6.0.0

websockets==13.0
python-dotenv>=1.0.0,<2.0.0
httpx>=0.24.0,<0.30.0
